name: Build

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.0.1

    - uses: actions/setup-node@v2.1.5
      with:
        node-version: 14.x

    - name: Build
      run: |
        npm ci
        npm run build